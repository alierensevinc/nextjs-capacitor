import {CapacitorConfig} from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.alierensevinc.nextcap',
    appName: 'next-cap',
    webDir: 'out',
    bundledWebRuntime: false
};

export default config;
